import { isArray, isDice, isScorecard } from "./validators";
import {
    sort,
    count,
    distinct,
    hasFullHouse,
    hasSequence,
    sum,
    sumIf
} from "./dice";

export {
    isArray,
    isDice,
    isScorecard,
    sort,
    count,
    distinct,
    hasFullHouse,
    hasSequence,
    sum,
    sumIf
};
